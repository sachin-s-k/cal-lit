spring:
  application:
    name: cal-lit-backend
  data:
    mongodb:
      uri: ${MONGODB_URI}

  logging:
    level:
      org.springframework: INFO


  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 300 #  5m
    refreshTokenExpiration: 604800 #  7 day